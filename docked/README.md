### Dockerized utilities.

- **i18n-diff** uses tree-sitter within docker and git at host - script shows i18n changes in PR in PWD
- **sass** uses Ruby Sass (v3.4.22) within Docker - compiles Zabbix SCSS themes to CSS in PWD
